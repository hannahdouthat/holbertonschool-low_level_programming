#include "main.h"

/**
 * _abs - absolute value
 * @i: is a variable 
 *
 * Return: Return is i
 */

int _abs(int i)
{
if (i > 0)
{
return (i);
}
else if (i < 0)
{
return (-i);
}
else
{
return (i);
}
}
